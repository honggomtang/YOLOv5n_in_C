# Python 관련
.venv/             # 가상환경
__pycache__/       # 파이썬 캐시
*.pyc              # 컴파일된 파이썬 파일

# 대용량 모델 파일
*.pt               # PyTorch 원본 모델
*.weights          # 바이너리 가중치
assets/weights*.h  # 헤더로 뽑은 가중치(엄청 큼)

# Vivado/Vitis 관련 임시 파일
.Xil/              # 자일링스 임시 로그
*.log              # 로그 파일
*.jou              # 저널 파일
*.str              # 전략 파일

# 빌드 결과물
obj/               # 컴파일된 오브젝트 파일
*.elf              # MicroBlaze 실행 파일
*.bit              # FPGA 비트스트림 파일 (필요시 제외 가능)
tests/test_conv0   # 로컬 테스트 실행파일